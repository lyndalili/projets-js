"use strict";

// Met dans cette variable un nombre aléatoire
let nombreAleatoire= Math.floor(Math.random()*100)

  //methode deux 
/*function getRandomNmbr(max) {
    return Math.floor(Math.random() * Math.floor(max));
  }
  console.log(getRandomNmbr(10));*/

// Vérifie dans la console que la variable `nombreAleatoire` contient bien un nombre aléatoire

console.log(nombreAleatoire);