let prenom = "kevin";
let age = 23;
let language = "css";

alert ("this is the story of " + prenom + ". " + prenom + " is " + age + " years old . he is a web devaloppeur and he favorit programing language " + language + ". " + prenom + "love's" + language + "so much that he use it evry day ");

console.log(prenom);


let favoriteMeals = [  'couscous' , 'lasage']

console.log (favoriteMeals [1]); //lasagne






















// Step 1 : Enable strict mode
"use strict";


// Step 2 : Declare a variable named 'favoriteMeals' and assign an array to it. Put the names of your favorite meals in this array.
let favoriteMeals = ["courscous", "Lasagne" , 'poulet teriyaki', "quiche lorraine", "raclette", "pot au feu", "beurgeur"];
console.log (favoriteMeals);
console.log(favoriteMeals[1]); //lasagne
console.log(favoriteMeals[2]); //poulet teriyaki ajo





/**
 * Part 1 : Reading data from an array
 */
// Display in the console the whole array

// Display in the console the first element of the array

// Display in the console the 2nd element of the array

// Display in the console the last element of the array (make sure that it still works even if you remove or add a new element in the array)

// Display in the console the 2nd last element of the array
 (favoriteMeals.length -2);
console.log [favoriteMeals.length -1];
// Display in the console the number of elements in the array
 console.log(favoriteMeals.length);

/**
 * Part 2 : Programmatically add elements in an array
 */


// Programmatically add an element at the end of the array and display the whole array in the console

(favoriteMeals.push('pizza'));
console.log (favoriteMeals)



// Programmatically add an element at the 3rd position of the array and display the whole array in the console

// Programmatically add an element at the beginning of the array and display the whole array in the console




/**
 * Part 3 : Programmatically remove elements from an array
 */
// Programmatically remove the last element of the array and display the whole array in the console

// Programmatically remove the 2nd element of the array and display the whole array in the console

// Programmatically remove the 1st element of the array and display the whole array in the console



/**
 * Part 4 : Programmatically edit elements of an array
 */
// Programmatically replace the 3rd element of the array by "pizza without ananas" and display the whole array in the console


/**
 * Part 5 : Miscellaneous
 */
// Display the index of the element "pizza without ananas" 

// Display all the elements, separated by a " - "

// Create a variable called "favorite_drinks" containing an array of your favorite drinks' names and display it in the console

// Concatenate the array "meals" and the array "drinks" into a new variable called "favoriteMealsAndDrinks" and display it in the console

// Reverse the array "favoriteMealsAndDrinks" and display it in the console

// Sort the array "favoriteMealsAndDrinks" in alphabetical order and display it in the console





