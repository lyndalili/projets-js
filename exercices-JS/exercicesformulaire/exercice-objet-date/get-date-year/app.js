"use strict";

// Dans cette variable se trouve la date actuelle
let dateActuelle = new Date();

// Récupère dans la variable `annee` l'année de la `dateActuelle`
let annee= dateActuelle.getFullYear()

// Vérifie dans la console que la variable `annee` contient bien l'année de la `dateActuelle`
console.log(dateActuelle);
console.log(annee);